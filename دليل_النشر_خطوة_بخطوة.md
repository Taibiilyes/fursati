# 🚀 دليل النشر على الإنترنت — خطوة بخطوة (بدون خبرة برمجية)

الهدف: منصة «فرصتي» تعمل على رابط دائم: **`https://fursati-dz.onrender.com`**
وهذا الرابط هو نفسه المثبّت داخل تطبيق الأندرويد `فرصتي.apk` — لذلك **التزم بالاسم `fursati-dz`** في الخطوة 4.

⏱ الوقت المطلوب: 10 دقائق • 💰 التكلفة: 0 (خطة مجانية)

---

## الخطوة 1 — حمّل ملفات المنصة

من مساحة العمل هنا، نزّل: **`فرصتي-المشروع_الكامل.zip`** وفك ضغطه على حاسوبك. ستجد مجلد `south-jobs`.

## الخطوة 2 — أنشئ حساباً مجانياً

1. حساب GitHub: ‏github.com ← Sign up (بريد + كلمة مرور + تأكيد)
2. حساب Render: ‏render.com ← **Sign up with GitHub** (أسهل)

## الخطوة 3 — ارفع ملفات المنصة إلى GitHub

1. في GitHub اضغط **+** أعلى اليمين ← **New repository**
2. الاسم: `fursati` ‏ ← اختر **Private** ← **Create repository**
3. في صفحة المستودع: **uploading an existing file** ← اسحب **محتويات** مجلد `south-jobs` كلها (app.py و db.py و… وكذلك `render.yaml` و `requirements.txt` و `Dockerfile`)
4. اضغط **Commit changes** وانتظر انتهاء الرفع

## الخطوة 4 — انشر على Render (الاسم حاسم!)

1. في Render: **New +** ← **Blueprint**
2. اختر مستودع `fursati` واربطه — سيقرأ Render ملف `render.yaml` تلقائياً
3. ✋ تأكد أن اسم الخدمة الظاهر هو **`fursati-dz`** (إن طلب تأكيداً اضغط Apply)
4. اضغط **Apply** وانتظر 3–5 دقائق

🎉 ستحصل على: **`https://fursati-dz.onrender.com`** — هذا دومين دائم يعمل من أي مكان.

> ⚠️ إذا كان الاسم `fursati-dz` محجوزاً سابقاً ولم يقبله Render: اختر اسماً آخر، وأخبرني بالرابط النهائي وسأعيد بناء `فرصتي.apk` عليه في دقيقة (مفتاح التوقيع محفوظ — التحديث يبقى متوافقاً).

## الخطوة 5 — التحقق

افتح الرابط على الهاتف ← ستظهر المنصة ← جرّب «ادخل مباشرة بحساب تجريبي جاهز».
**ملاحظة:** الخطة المجانية «تنام» بعد 15 دقيقة بلا زيارات — أول فتح بعدها يتأخر ~50 ثانية ثم يعمل طبيعياً.

## الخطوة 6 — ثبّت التطبيق وشاركه

**طريقتان للتثبيت على الهاتف:**

| الطريقة | الخطوات |
|---|---|
| 📲 **APK** (الملف الجاهز) | انقل `فرصتي.apk` للأندرويد (واتساب/بلوتوث/كيبل) ← افتحه ← اسمح بـ«تثبيت من مصادر غير معروفة» ← افتح التطبيق من أيقونته |
| 🌐 **PWA** (بدون ملف) | افتح `fursati-dz.onrender.com` من كروم ← شريط «📲 ثبّت فرصتي» ← تثبيت |

شارك ملف APK مع أصدقائك مباشرة — كلهم سيدخلون بنفس الرابط، وكل واحد يُنشئ حسابه بسيرته الذاتية.

---

## 🔒 ملاحظات تشغيلية

- **قاعدة البيانات:** الخطة المجانية في Render تمسح الملفات عند كل إعادة نشر/إسبات. للاستخدام العائلي اقبل ذلك، أو فعّل **Persistent Disk** (مدفوع رمزياً) من لوحة Render ويُثبَّت على `/opt/data` (أخبرني لأعدّل المسارات).
- **كلمات مرور SMTP:** محفوظة في قاعدة البيانات على الخادم — لأمان أعلى فعّل القرص الدائم ولا تشارك لوحة Render مع أحد.
- **مفتاح توقيع APK:** مجلد `apk-signing/` غير مرفوع إلى GitHub (مستبعد تلقائياً) — احتفظ به في مكان آمن؛ كل تحديث مستقبلي للتطبيق يجب أن يُوقّع بنفس المفتاح.
- **الترقية لاحقاً:** عدّل الكود ← ارفع التغييرات على GitHub ← Render يعيد النشر تلقائياً.
